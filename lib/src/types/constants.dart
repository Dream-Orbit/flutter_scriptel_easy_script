import 'package:flutter/material.dart';

const DEVICE_SCREEN_WIDTH = 240;
const DEVICE_SCREEN_HEIGHT = 64;
const DEVICE_ASPECT_RATIO = DEVICE_SCREEN_WIDTH / DEVICE_SCREEN_HEIGHT;

const Color primaryColor = Color(0xFF283593);
